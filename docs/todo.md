# 개발 TODO

현재 우선순위는 `개요 / 월간분석 / 수입분석 / 지출분석` 4개 화면의 완성도를 높이는 것이다.

## 지금 먼저 할 일

### 1. 라이트 모드 마감

- `거래`, `계정` 숨김 화면까지 라이트 테마를 같은 기준으로 맞춘다.
- 라이트 모드에서 남아 있는 하드코딩 색상(`text-white`, `bg-stone-*`)을 추가로 제거한다.
- 차트 색상 대비를 `Wealthfolio` 톤에 더 가깝게 다듬는다.

### 2. 설정 기능 확장

- 설정 팝오버에 `인트로 다시 보기`를 추가한다.
- 다크/라이트 선택값 외에 UI 설정을 어디까지 저장할지 정한다.
- 설정 패널 닫힘 동작과 모바일 터치 UX를 한 번 더 점검한다.

### 3. 인트로 오버레이 검토

- 현재 첫 방문 오버레이가 실제로 필요한지 사용감 기준으로 판단한다.
- 유지한다면 문구, 제목, 버튼 톤을 더 다듬는다.
- 불필요하면 `App` 레벨에서 바로 롤백한다.

## 그다음 할 일

### 4. 월간분석 데이터 최적화

- `items_of_account_id.json` 반복 호출 수를 줄일 방법을 검토한다.
- `changes_of_account_id.json` 반복 호출에 캐시나 병렬화가 필요한지 본다.
- 월간 보고서 응답 시간을 실제 기준으로 측정하고 병목을 기록한다.

### 5. 수입/지출 분석 polish

- `최근 1년 / 작년 / 올해` preset 명칭이 충분히 직관적인지 다시 본다.
- 막대/누적선 차트의 축 라벨과 툴팁 문구를 더 정돈한다.
- 필요하면 `전년 동기`, `전월 대비` 같은 보조 인사이트를 추가한다.

### 6. 월간분석 polish

- `월 수익 구성`도 `수익 -> 수입` 용어로 통일할지 결정한다.
- `월 아이템 목록`과 `월 지출 구성`의 시각 밀도를 한 번 더 조정한다.
- 이벤트 포인트 라벨이 많은 달에 과밀해지는지 확인한다.

## 배포/운영

### 7. Docker 빌드 안정화

- 현재 Docker 빌드는 `go mod download` 단계에서 `proxy.golang.org` 타임아웃으로 실패할 수 있다.
- Docker 환경에서 Go 모듈 네트워크가 왜 막히는지 원인 확인이 필요하다.
- 우회 방식을 쓰더라도 Dockerfile에는 과한 임시 설정을 남기지 않도록 정리한다.

### 8. 문서 정리

- 현재 화면 구조와 용어(`수입`, `지출`)를 README에 반영한다.
- 라이트/다크 모드와 첫 방문 오버레이 동작을 짧게 문서화한다.
- 배포 절차와 환경변수 설명을 실제 현재 구조 기준으로 다시 맞춘다.
