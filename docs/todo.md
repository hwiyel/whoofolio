# 개발 TODO

현재 우선순위는 새 화면을 늘리는 것보다 `개요`와 `거래` 두 화면의 완성도를 높이는 데 있다.

## 지금 먼저 할 일

### 1. 대시보드 polish

- 큰 순자산 차트의 축, 툴팁, 색감, 기간 버튼 스타일을 정리한다.
- 핵심 카드 위계를 다시 잡는다.
- 우선 카드 후보는 `순자산`, `이번 달 수입`, `이번 달 지출`, `순현금흐름`이다.

### 2. 거래 필터 개선

- 기간 preset을 추가한다. 예: `이번 주`, `이번 달`, `최근 3개월`
- 키워드 검색 입력 UX를 다듬는다.
- 필터 초기화와 기본값 동작을 더 분명하게 만든다.

### 3. 거래 테이블 정리

- `잔액` 컬럼이 실제로 필요한지 검토한다.
- 메모는 한 줄 미리보기로 둘지, 아이콘 중심으로 줄일지 결정한다.
- `계정 흐름` 문구를 더 자연스럽게 다듬는다.

## 그다음 할 일

### 4. 계정 뷰 추가

- `개요`, `거래` 다음 화면으로 `계정` 뷰를 추가한다.
- 계정별 잔액 변화와 최근 거래 진입점을 제공한다.

### 5. 캐시 전략 정리

- 계정 목록, 대시보드 요약, 추이 데이터에 서버 캐시를 붙인다.
- 프론트 `localStorage`는 최근 필터, 마지막 기간, 컬럼 표시 상태 같은 UI 상태 저장에만 쓴다.

## 배포 전 정리

### 6. 운영 설정 문서화

- `.env` 기준 필수 환경변수를 다시 정리한다.
- Docker Compose와 Nginx Proxy Manager 연결 방식을 문서로 고정한다.

### 7. 빈 상태와 에러 상태

- 후잉 API 실패 메시지를 사용자 관점에서 다시 쓴다.
- 데이터 없는 기간 선택 시 안내 문구를 정리한다.
- 로딩 상태를 더 차분하게 다듬는다.
